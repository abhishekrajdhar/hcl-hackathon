# --- Web framework ---------------------------------------------------------
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# --- Validation / settings -------------------------------------------------
pydantic==2.10.4
pydantic-settings==2.7.0
email-validator==2.2.0

# --- Database --------------------------------------------------------------
SQLAlchemy[asyncio]==2.0.36
asyncpg==0.30.0
psycopg2-binary==2.9.10
alembic==1.14.0
pgvector==0.3.6
greenlet==3.1.1

# --- Cache -----------------------------------------------------------------
# Optional at runtime: EMBEDDING_CACHE_BACKEND=memory needs none of this, and
# the Redis cache degrades to in-process if the server is unreachable.
redis==5.2.1

# --- Security --------------------------------------------------------------
PyJWT==2.10.1
bcrypt==4.2.1

# --- LLM providers ---------------------------------------------------------
# Selected at runtime via LLM_PROVIDER; imported lazily so the app runs without
# them when using the default mock provider.
anthropic==0.44.0
openai==1.59.6

# --- Catalogue ingestion ---------------------------------------------------
# Used by the catalogue providers (YouTube Data API / scrape fallback). Already
# an indirect dependency of fastapi and openai; pinned here because app code now
# imports it directly.
httpx==0.28.1
