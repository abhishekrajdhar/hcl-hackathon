# Host-side port mappings for docker compose.
# Change these if the defaults are already in use on your machine.
POSTGRES_PORT=5432
REDIS_PORT=6379
API_PORT=8000

# Credentials for the postgres container (must match backend/.env)
POSTGRES_USER=lpr
POSTGRES_PASSWORD=lpr
POSTGRES_DB=lpr
